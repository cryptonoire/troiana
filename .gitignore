node_modules/
.DS_Store
*.log
.vercel

# Admin secrets & stored data (never commit)
config.php
# Private runtime data, but ship the CMS content source.
data/*
!data/content.json
# Backups written by the admin publisher
*.bak
